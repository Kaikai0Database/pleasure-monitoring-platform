# 🔧 快速啟動指南

## ⚡ 最簡單的方法（推薦）

**雙擊執行：** `啟動所有服務.bat`

這會自動啟動所有三個服務：
- ✅ 後端服務器 (端口 5000)
- ✅ 主前端 (端口 5173)
- ✅ 管理後台 (端口 5174)

等待 5-10 秒後，打開瀏覽器訪問：
- **管理後台**: http://localhost:5174

---

## 📋 診斷結果

### 目前狀況
- ❌ 端口 5000 無監聽（後端未啟動）
- ❌ 端口 5173 無監聽（主前端未啟動）
- ❌ 端口 5174 無監聽（管理後台未啟動）

這就是為什麼瀏覽器顯示「無法連上伺服器」。

---

## 🛠️ 手動啟動（如果腳本無效）

### 方法 1: 使用 PowerShell

打開 **3 個** PowerShell 視窗，分別執行：

**視窗 1 - 後端：**
```powershell
cd c:\Users\user\Desktop\pleasure-monitoring-platform_NEW\backend
python run.py
```

**視窗 2 - 主前端：**
```powershell
cd c:\Users\user\Desktop\pleasure-monitoring-platform_NEW
npm run dev
```

**視窗 3 - 管理後台：**
```powershell
cd c:\Users\user\Desktop\pleasure-monitoring-platform_NEW\admin-frontend
npm run dev
```

### 方法 2: 使用單獨的 .bat 檔案

依序雙擊執行：
1. `backend\start_backend.bat`
2. `start_main_frontend.bat`
3. `start_admin_frontend.bat`

---

## ✅ 驗證服務已啟動

### 檢查端口監聽

在 PowerShell 執行：
```powershell
netstat -ano | findstr ":5000 :5173 :5174"
```

應該看到類似：
```
TCP    0.0.0.0:5000    0.0.0.0:0    LISTENING    xxxx
TCP    [::]:5173       [::]:0       LISTENING    xxxx
TCP    [::]:5174       [::]:0       LISTENING    xxxx
```

### 測試後端

訪問：http://localhost:5000/api/health

應該看到：`{"status": "ok", "message": "Flask backend is running"}`

---

## 🚨 常見問題

### 問題：腳本執行後立即關閉

**原因：** 可能缺少依賴或路徑錯誤

**解決：** 使用手動啟動方法，查看錯誤訊息

### 問題：端口已被佔用

**解決：** 終止佔用的進程
```powershell
# 查找進程
netstat -ano | findstr :5174

# 終止進程（替換 PID）
taskkill /PID <PID> /F
```

### 問題：npm 命令未找到

**解決：** 確認已安裝 Node.js，並重新啟動命令提示字元
