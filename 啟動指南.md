# 失樂症監測平台 - 完整啟動指南

## 🚀 快速啟動（使用腳本）

### 步驟 1: 啟動後端服務器

📂 雙擊執行：`backend\start_backend.bat`

**預期輸出：**
```
* Running on http://127.0.0.1:5000
* Debug mode: on
```

### 步驟 2: 啟動主前端（用戶界面）

📂 雙擊執行：`start_main_frontend.bat`

**預期輸出：**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5173/
```

### 步驟 3: 啟動管理後台（醫護人員界面）

📂 雙擊執行：`start_admin_frontend.bat`

**預期輸出：**
```
VITE v5.x.x  ready in xxx ms
➜  Local:   http://localhost:5174/
```

---

## 📝 重要提醒

1. **必須保持三個視窗都開啟** - 關閉任何一個都會導致該服務停止
2. **啟動順序建議**：先啟動後端，再啟動前端
3. **停止服務**：在各視窗按 `Ctrl+C` 然後關閉視窗

---

## 🌐 訪問地址

啟動完成後，訪問以下網址：

- **主前端（用戶）**: http://localhost:5173
- **管理後台（醫護）**: http://localhost:5174  
- **後端 API**: http://localhost:5000

---

## ❓ 故障排除

### 問題：端口已被佔用

**錯誤訊息：** `Error: listen EADDRINUSE: address already in use :::5173`

**解決方法：**
```powershell
# 查找佔用端口的進程
netstat -ano | findstr :5173

# 終止該進程（將 PID 替換為實際值）
taskkill /PID <PID> /F
```

### 問題：模組未找到

**錯誤訊息：** `ModuleNotFoundError` 或 `Cannot find module`

**解決方法：**

後端：
```powershell
cd backend
pip install -r requirements.txt
```

前端：
```powershell
# 主前端
npm install

# 管理前端
cd admin-frontend
npm install
```

---

## 📂 所有啟動腳本位置

```
pleasure-monitoring-platform_NEW/
├── backend/
│   └── start_backend.bat          ← 後端服務器
├── start_main_frontend.bat        ← 主前端
└── start_admin_frontend.bat       ← 管理後台
```
